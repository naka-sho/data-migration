CREATE TABLE your_table_name (
    myid SERIAL PRIMARY KEY,
    named VARCHAR,
    named_s VARCHAR(8),
    x_flag BOOLEAN,
    rate DOUBLE PRECISION,
    score BIGINT ,
    time TIMESTAMP,
    purchase DATE
);
